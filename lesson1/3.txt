<?php
    $a = 5;
    $b = '05';
    var_dump($a == $b);         // Почему true? - сравнение по значению, 5=5.
    var_dump((int)'012345');     // Почему 12345? - integer (целые числа), числа не начинаются с 0
    var_dump((float)123.0 === (int)123.0); // Почему false? - 
	с одной стороны число с плавающей точкой, с другой стороны целое число, объявлен разный тип данных, а совпадение должно быть полное.
    var_dump((int)0 === (int)'hello, world'); // Почему true? - типы данных одинаковые - целое число. 0 - не целое число, 'hello, world' тоже
?>